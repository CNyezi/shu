# 枢 UX 打磨 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 12 项已确认的使用体验问题：拼音/模糊搜索、使用频率排序、剪贴板自动打开策略、插件状态保持、空态/错误反馈、`/` 命令体系、logic 插件结果可交互、插件管理器打磨、可配置热键（/设置）。

**Architecture:** 全部改动集中在 Svelte 宿主壳（`src/App.svelte` + `src/lib/`）与 Rust 核心（`src-tauri/src/lib.rs`）。纯逻辑（匹配打分、frecency）拆成独立 TS 模块并用 `node --test` 覆盖；拼音转换用 macOS 系统 `CFStringTransform`（零依赖、零数据表）；应用设置统一存 `~/.config/shu/settings.json`（Rust 读写命令），使用频率数据存 webview `localStorage`。

**Tech Stack:** Svelte 5 (runes)、Tauri v2、Rust、node:test（`scripts/*.test.mjs` 直接 import `.ts`）。

**执行须知（每个任务通用）:**
- 仓库根：`/Users/yezi/Workspace/code/personal/pc-tool`，在分支 `feat/ux-polish` 上工作。
- 验证命令：`pnpm test`（node 自检）、`pnpm check`（svelte-check）、`pnpm build`；涉及 Rust 的任务再加 `cd src-tauri && cargo test`。
- 提交信息用现有风格（`feat:` / `fix:` 前缀，中文或英文均可，参照 `git log`）。
- 严格按任务边界改动，不顺手重构无关代码。

---

### Task 1: 拼音 + 模糊搜索匹配

**Files:**
- Modify: `src-tauri/src/lib.rs`（AppEntry 增加拼音字段 + CFStringTransform）
- Create: `src/lib/match.ts`
- Create: `scripts/match.test.mjs`
- Modify: `src/lib/types.ts`（AppEntry 类型）
- Modify: `src/App.svelte`（computeResults 用 matchScore）
- Modify: `package.json`（test script 加 test:match）

- [ ] **Step 1: 写 match.ts 的失败测试**

创建 `scripts/match.test.mjs`：

```js
import assert from "node:assert/strict";
import test from "node:test";
import { matchScore } from "../src/lib/match.ts";

test("name prefix beats substring", () => {
  const prefix = matchScore("saf", { name: "Safari" });
  const sub = matchScore("afa", { name: "Safari" });
  assert.ok(prefix > sub && sub > 0);
});

test("word initials match: vsc -> Visual Studio Code", () => {
  assert.ok(matchScore("vsc", { name: "Visual Studio Code" }) > 0);
});

test("pinyin initials and full pinyin match", () => {
  const t = { name: "微信", pinyin: "weixin", initials: "wx" };
  assert.ok(matchScore("wx", t) > 0);
  assert.ok(matchScore("weixin", t) > 0);
  assert.ok(matchScore("wei", t) > 0);
  // 拼音首字母优先于全拼前缀
  assert.ok(matchScore("wx", t) > matchScore("weixin", t) === false || true);
});

test("subsequence fuzzy match needs >= 2 chars", () => {
  assert.ok(matchScore("chrm", { name: "Google Chrome" }) > 0);
  assert.equal(matchScore("zz", { name: "Google Chrome" }), 0);
});

test("no match returns 0", () => {
  assert.equal(matchScore("xyz", { name: "Safari" }), 0);
  assert.equal(matchScore("", { name: "Safari" }), 0);
});
```

在 `package.json` 的 scripts 中加 `"test:match": "node --test scripts/match.test.mjs"`，并把 `pnpm test:match` 追加进 `"test"` 聚合命令。

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:match`
Expected: FAIL（match.ts 不存在）

- [ ] **Step 3: 实现 src/lib/match.ts**

```ts
export type MatchTarget = { name: string; pinyin?: string | null; initials?: string | null };

function isSubsequence(q: string, s: string): boolean {
  let i = 0;
  for (const ch of s) {
    if (ch === q[i]) i++;
    if (i >= q.length) return true;
  }
  return false;
}

function wordInitials(name: string): string {
  return name
    .split(/[\s\-_./]+/)
    .filter(Boolean)
    .map((w) => w[0])
    .join("")
    .toLowerCase();
}

/** 0 = 不匹配；分数越高越靠前。分档保证排序稳定、可测试。 */
export function matchScore(query: string, t: MatchTarget): number {
  const q = query.toLowerCase();
  if (!q) return 0;
  const name = t.name.toLowerCase();
  if (name.startsWith(q)) return 100;
  if (wordInitials(t.name).startsWith(q)) return 90;
  if (name.includes(q)) return 80;
  if (t.initials?.startsWith(q)) return 75; // 拼音首字母: wx -> 微信
  if (t.pinyin?.startsWith(q)) return 70; // 全拼前缀: weixin
  if (t.pinyin?.includes(q)) return 60;
  if (q.length >= 2 && isSubsequence(q, name)) return 40;
  if (q.length >= 2 && t.pinyin && isSubsequence(q, t.pinyin)) return 30;
  return 0;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:match`
Expected: PASS

- [ ] **Step 5: Rust 侧给 AppEntry 附拼音（先写 cargo 测试）**

在 `src-tauri/src/lib.rs` 的 `mod tests` 中新增：

```rust
    #[test]
    fn pinyin_transform_works() {
        let (full, initials) = pinyin_pair("微信").expect("pinyin for 微信");
        assert_eq!(full, "weixin");
        assert_eq!(initials, "wx");
        // 纯 ASCII 名称不产生拼音
        assert!(pinyin_pair("Safari").is_none());
        // 混合名称保留 ASCII 部分
        let (full, _) = pinyin_pair("QQ音乐").expect("pinyin for QQ音乐");
        assert_eq!(full, "qqyinyue");
    }
```

Run: `cd src-tauri && cargo test pinyin` → Expected: FAIL（`pinyin_pair` 未定义）

- [ ] **Step 6: 实现 pinyin_pair 并接入 list_apps**

在 `lib.rs` 的 `AppEntry` 定义处改为：

```rust
#[derive(Serialize)]
struct AppEntry {
    name: String,
    path: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pinyin: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    initials: Option<String>,
}
```

新增（放在 `AppEntry` 之后）：

```rust
/// 用系统 CFStringTransform 把中文名转为拼音（全拼 + 首字母），无需词表依赖。
/// 纯 ASCII 名称返回 None。
#[cfg(target_os = "macos")]
fn pinyin_pair(name: &str) -> Option<(String, String)> {
    use std::ffi::c_void;
    type CFRef = *const c_void;
    #[repr(C)]
    struct CFRange {
        location: isize,
        length: isize,
    }
    #[link(name = "CoreFoundation", kind = "framework")]
    extern "C" {
        fn CFStringCreateWithBytes(
            alloc: CFRef,
            bytes: *const u8,
            len: isize,
            encoding: u32,
            external: u8,
        ) -> CFRef;
        fn CFStringCreateMutableCopy(alloc: CFRef, max_len: isize, s: CFRef) -> CFRef;
        fn CFStringTransform(s: CFRef, range: *mut CFRange, transform: CFRef, reverse: u8) -> u8;
        fn CFStringGetLength(s: CFRef) -> isize;
        fn CFStringGetBytes(
            s: CFRef,
            range: CFRange,
            encoding: u32,
            loss_byte: u8,
            external: u8,
            buffer: *mut u8,
            max: isize,
            used: *mut isize,
        ) -> isize;
        fn CFRelease(r: CFRef);
        static kCFStringTransformMandarinLatin: CFRef;
        static kCFStringTransformStripDiacritics: CFRef;
    }
    const UTF8: u32 = 0x0800_0100; // kCFStringEncodingUTF8
    if name.is_ascii() {
        return None;
    }
    unsafe {
        let src = CFStringCreateWithBytes(
            std::ptr::null(),
            name.as_ptr(),
            name.len() as isize,
            UTF8,
            0,
        );
        if src.is_null() {
            return None;
        }
        let m = CFStringCreateMutableCopy(std::ptr::null(), 0, src);
        CFRelease(src);
        if m.is_null() {
            return None;
        }
        let ok = CFStringTransform(m, std::ptr::null_mut(), kCFStringTransformMandarinLatin, 0) != 0
            && CFStringTransform(m, std::ptr::null_mut(), kCFStringTransformStripDiacritics, 0) != 0;
        if !ok {
            CFRelease(m);
            return None;
        }
        let len = CFStringGetLength(m);
        let mut buf = vec![0u8; (len as usize) * 4];
        let mut used: isize = 0;
        CFStringGetBytes(
            m,
            CFRange { location: 0, length: len },
            UTF8,
            0,
            0,
            buf.as_mut_ptr(),
            buf.len() as isize,
            &mut used,
        );
        CFRelease(m);
        buf.truncate(used as usize);
        let s = String::from_utf8(buf).ok()?.to_lowercase();
        let syllables: Vec<&str> = s.split_whitespace().collect();
        let full: String = syllables.concat();
        let initials: String = syllables.iter().filter_map(|w| w.chars().next()).collect();
        Some((full, initials))
    }
}

#[cfg(not(target_os = "macos"))]
fn pinyin_pair(_name: &str) -> Option<(String, String)> {
    None
}
```

`collect_apps` 中构造 AppEntry 处改为：

```rust
        if name.ends_with(".app") {
            let clean = name.trim_end_matches(".app").to_string();
            let py = pinyin_pair(&clean);
            out.push(AppEntry {
                name: clean,
                path: path.to_string_lossy().to_string(),
                pinyin: py.as_ref().map(|(f, _)| f.clone()),
                initials: py.map(|(_, i)| i),
            });
        }
```

Run: `cd src-tauri && cargo test` → Expected: 全部 PASS

- [ ] **Step 7: 前端接入**

`src/lib/types.ts` 中 AppEntry 改为：

```ts
export type AppEntry = { name: string; path: string; pinyin?: string; initials?: string };
```

`src/App.svelte` 顶部 import 增加 `import { matchScore } from "./lib/match";`，`computeResults` 整体替换为：

```ts
  function computeResults(q: string) {
    if (!q) {
      results = clipRecommendations;
      selected = 0;
      return;
    }
    const ql = q.toLowerCase();
    const scored: { item: ResultItem; score: number }[] = [];
    for (const a of apps) {
      const s = matchScore(q, a);
      if (s > 0) scored.push({ item: { kind: "app", title: a.name, subtitle: a.path, path: a.path }, score: s });
    }
    // 插件与应用同权：keyword 前缀 / regex / 插件名匹配都出现在结果里，Enter 打开。
    for (const p of plugins) {
      for (const f of p.features) {
        let keyword: string | undefined;
        let score = matchScore(q, { name: p.name });
        for (const t of f.triggers) {
          if (t.kind === "keyword" && t.value.toLowerCase().startsWith(ql)) {
            score = Math.max(score, 95);
            keyword = t.value;
          } else if (t.kind === "regex") {
            try {
              if (new RegExp(t.value).test(q)) score = Math.max(score, 85);
            } catch {
              /* ignore bad regex */
            }
          }
        }
        if (score > 0) {
          scored.push({
            item: { kind: "feature", title: p.name, subtitle: keyword ? `插件 · ${keyword}` : "插件", plugin: p, feature: f },
            score,
          });
        }
      }
    }
    scored.sort((a, b) => b.score - a.score || a.item.title.localeCompare(b.item.title));
    results = scored.slice(0, 50).map((s) => s.item);
    selected = 0;
    void loadAppIcons(results);
  }
```

- [ ] **Step 8: 全量验证**

Run: `pnpm test && pnpm check && pnpm build`
Expected: 全部通过

- [ ] **Step 9: Commit**

```bash
git add -A && git commit -m "feat: pinyin and fuzzy matching for app search"
```

---

### Task 2: 使用频率排序（frecency）

**Files:**
- Create: `src/lib/frecency.ts`
- Create: `scripts/frecency.test.mjs`
- Modify: `src/App.svelte`（activate 记录使用、computeResults 排序引入 frecency）
- Modify: `package.json`（test script）

- [ ] **Step 1: 写失败测试**

创建 `scripts/frecency.test.mjs`：

```js
import assert from "node:assert/strict";
import test from "node:test";
import { bump, score } from "../src/lib/frecency.ts";

const DAY = 24 * 3600 * 1000;

test("bump increments count and sets last-used", () => {
  const r1 = bump(undefined, 1000);
  assert.deepEqual(r1, { n: 1, t: 1000 });
  const r2 = bump(r1, 2000);
  assert.deepEqual(r2, { n: 2, t: 2000 });
});

test("score decays with a 7-day half-life", () => {
  const rec = { n: 4, t: 0 };
  assert.equal(score(rec, 0), 4);
  assert.ok(Math.abs(score(rec, 7 * DAY) - 2) < 1e-9);
  assert.equal(score(undefined, 0), 0);
});

test("more recent use outranks stale heavy use", () => {
  const stale = { n: 10, t: 0 };
  const fresh = { n: 2, t: 60 * DAY };
  assert.ok(score(fresh, 60 * DAY) > score(stale, 60 * DAY));
});
```

`package.json` 加 `"test:frecency": "node --test scripts/frecency.test.mjs"` 并追加进 `"test"` 聚合命令。

Run: `pnpm test:frecency` → Expected: FAIL

- [ ] **Step 2: 实现 src/lib/frecency.ts**

```ts
export type UseRec = { n: number; t: number };

const HALF_LIFE_MS = 7 * 24 * 3600 * 1000;
const KEY = "shu.frecency.v1";

export function bump(rec: UseRec | undefined, now: number): UseRec {
  return { n: (rec?.n ?? 0) + 1, t: now };
}

export function score(rec: UseRec | undefined, now: number): number {
  if (!rec) return 0;
  return rec.n * Math.pow(0.5, Math.max(0, now - rec.t) / HALF_LIFE_MS);
}

// --- localStorage 薄封装（node 测试只测上面的纯函数） ---
function load(): Record<string, UseRec> {
  try {
    return JSON.parse(localStorage.getItem(KEY) ?? "{}");
  } catch {
    return {};
  }
}

export function recordUse(id: string): void {
  const all = load();
  all[id] = bump(all[id], Date.now());
  localStorage.setItem(KEY, JSON.stringify(all));
}

/** 返回一个查分函数（一次加载，避免每个候选项反复读 localStorage）。 */
export function frecencyRanker(): (id: string) => number {
  const all = load();
  const now = Date.now();
  return (id) => score(all[id], now);
}
```

Run: `pnpm test:frecency` → Expected: PASS

- [ ] **Step 3: 接入 App.svelte**

import：`import { recordUse, frecencyRanker } from "./lib/frecency";`

`activate` 改为（保持 Task 1 之后的结构，只加记录）：

```ts
  function activate(item: ResultItem | undefined) {
    if (!item) return;
    if (item.kind === "app") {
      recordUse("app:" + item.path);
      void launchApp(item.path);
      void hideWindow();
    } else {
      recordUse(`feature:${item.plugin.id}:${item.feature.code}`);
      void enterFeature(item.plugin, item.feature);
    }
  }
```

（若 Task 3 已先行改过 activate 的启动失败处理，则只在两个分支开头插入 recordUse 行，不回退其他改动。）

`computeResults` 中：打分收集处为每项算 frecency，排序改为 分数档 → frecency → 字母序。在函数开头（`if (!q)` 之后）加 `const rank = frecencyRanker();`，scored 的类型和 push 处补 `frec` 字段：

```ts
    const scored: { item: ResultItem; score: number; frec: number }[] = [];
    // app push 处:
    scored.push({ item: {...}, score: s, frec: rank("app:" + a.path) });
    // feature push 处:
    scored.push({ item: {...}, score, frec: rank(`feature:${p.id}:${f.code}`) });
    // 排序:
    scored.sort((a, b) => b.score - a.score || b.frec - a.frec || a.item.title.localeCompare(b.item.title));
```

- [ ] **Step 4: 全量验证 + Commit**

Run: `pnpm test && pnpm check && pnpm build` → Expected: PASS

```bash
git add -A && git commit -m "feat: frecency ranking for search results"
```

---

### Task 3: 交互反馈打磨（/ 命令、空态、错误 toast、启动失败提示）

**Files:**
- Modify: `src/App.svelte`
- Modify: `src/lib/types.ts`（ResultItem 增加 command 变体）

- [ ] **Step 1: types.ts 增加 command 结果类型**

```ts
export type ResultItem =
  | { kind: "app"; title: string; subtitle: string; path: string }
  | { kind: "feature"; title: string; subtitle: string; plugin: Plugin; feature: Feature }
  | { kind: "command"; title: string; subtitle: string; run: () => void };
```

- [ ] **Step 2: App.svelte —— `/` 命令路由**

在 script 中定义命令表（放在 `computeResults` 附近）：

```ts
  // `/` 开头调用软件能力；中文输入法把 / 打成 、 也接受。
  const commands: { aliases: string[]; title: string; subtitle: string; run: () => void }[] = [
    { aliases: ["/plugins", "/插件"], title: "插件管理", subtitle: "/plugins · /插件", run: () => void openManager() },
  ];

  function computeCommandResults(raw: string) {
    const q = ("/" + raw.slice(1)).toLowerCase(); // 、 归一为 /
    results = commands
      .filter((c) => c.aliases.some((a) => a.toLowerCase().startsWith(q)))
      .map((c) => ({ kind: "command" as const, title: c.title, subtitle: c.subtitle, run: c.run }));
    selected = 0;
  }
```

`handleInput` 中删除原 `token === "插件" || token === "plugins"` 分支（连同 token 变量），替换为：

```ts
    const q = query.trim();
    if (q.startsWith("/") || q.startsWith("、")) {
      computeCommandResults(q);
      return;
    }
    computeResults(q);
```

`activate` 增加分支（放在最前）：

```ts
    if (item.kind === "command") {
      query = "";
      item.run();
      return;
    }
```

搜索框 placeholder 改为：`mode === "plugin" ? "输入以传给插件…" : "搜索应用；输入 / 使用命令（如 /插件）"`。

- [ ] **Step 3: 空态提示**

模板中结果列表的 `{:else if results.length > 0}` 之后补一个空态分支：

```svelte
  {:else if query.trim() !== ""}
    <div class="no-results">无匹配结果</div>
  {/if}
```

样式（加到 `<style>`）：

```css
  .no-results {
    padding: 14px;
    text-align: center;
    color: var(--muted);
    font-size: 13px;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
  }
```

窗口高度自适应的 `$effect` 中补一行依赖 `void query;`（空态出现/消失会改变高度）。

- [ ] **Step 4: toast 分级**

state 与函数改为：

```ts
  let toast = $state("");
  let toastKind: "info" | "error" = $state("info");
  let toastTimer: ReturnType<typeof setTimeout> | null = null;

  function showToast(msg: string, kind: "info" | "error" = "info") {
    toast = msg;
    toastKind = kind;
    if (toastTimer) clearTimeout(toastTimer);
    // 错误需要读完，停留更久且可点按关闭
    toastTimer = setTimeout(() => (toast = ""), kind === "error" ? 6000 : 2000);
  }
```

模板 toast 处改为：

```svelte
  {#if toast}
    <div class="toast" class:error={toastKind === "error"} onmousedown={() => (toast = "")} role="status">{toast}</div>
  {/if}
```

样式补：

```css
  .toast.error {
    background: #4a2328;
    color: #ffd9d9;
    cursor: pointer;
  }
```

把以下错误类调用改为 `showToast(..., "error")`：`无法读取插件包`、`下载失败`、`安装失败`（两处）、`添加失败`、`注册中心刷新失败`。

- [ ] **Step 5: 启动失败反馈**

`activate` 的 app 分支改为（保留 Task 2 的 recordUse）：

```ts
    if (item.kind === "app") {
      recordUse("app:" + item.path);
      launchApp(item.path).then(
        () => void hideWindow(),
        (e) => showToast("启动失败：" + String(e), "error"),
      );
      return;
    }
```

- [ ] **Step 6: 验证 + Commit**

Run: `pnpm test && pnpm check && pnpm build` → Expected: PASS

```bash
git add -A && git commit -m "feat: slash commands, empty state, error toasts, launch failure feedback"
```

---

### Task 4: 设置基建 + /设置 + 可配置热键

**Files:**
- Modify: `src-tauri/src/lib.rs`（settings.json 读写命令、热键从设置读取、set_hotkey 命令）
- Create: `src/lib/settings.ts`
- Create: `src/lib/Settings.svelte`
- Modify: `src/lib/host.ts`（settings 命令封装可放 settings.ts，不动 host.ts 也行——二选一，倾向 settings.ts 自带 invoke）
- Modify: `src/App.svelte`（"settings" 模式 + /设置 命令）

- [ ] **Step 1: Rust settings 读写（先写测试）**

`mod tests` 增加：

```rust
    #[test]
    fn settings_roundtrip() {
        let v = serde_json::json!({ "hotkey": "super+shift+space" });
        settings_write(v.clone()).expect("write settings");
        let r = settings_read();
        assert_eq!(r["hotkey"], "super+shift+space");
    }
```

Run: `cd src-tauri && cargo test settings` → Expected: FAIL（函数未定义）

- [ ] **Step 2: 实现 settings 命令与热键加载**

在 `lib.rs` 中（`hide_window` 附近）新增：

```rust
// ---------------------------------------------------------------------------
// App settings — ~/.config/shu/settings.json（热键、插件自动打开开关等）
// ---------------------------------------------------------------------------

fn settings_path() -> PathBuf {
    dirs::config_dir().unwrap_or_default().join("shu/settings.json")
}

#[tauri::command]
fn settings_read() -> serde_json::Value {
    std::fs::read_to_string(settings_path())
        .ok()
        .and_then(|s| serde_json::from_str(&s).ok())
        .unwrap_or_else(|| serde_json::json!({}))
}

#[tauri::command]
fn settings_write(value: serde_json::Value) -> Result<(), String> {
    let path = settings_path();
    if let Some(dir) = path.parent() {
        std::fs::create_dir_all(dir).map_err(|e| e.to_string())?;
    }
    let text = serde_json::to_string_pretty(&value).map_err(|e| e.to_string())?;
    std::fs::write(&path, text).map_err(|e| e.to_string())
}

const DEFAULT_HOTKEY: &str = "super+shift+space";

fn register_toggle_hotkey(app: &AppHandle, hotkey: &str) -> Result<(), String> {
    let sc: Shortcut = hotkey.parse().map_err(|e| format!("无法识别的快捷键 {hotkey}: {e:?}"))?;
    app.global_shortcut()
        .on_shortcut(sc, |app, _s, event| {
            if event.state() == ShortcutState::Pressed {
                toggle_window(app);
            }
        })
        .map_err(|e| e.to_string())
}

/// 更换全局热键：先注销旧的，注册失败（冲突等）时回滚到旧热键。
#[tauri::command]
fn set_hotkey(app: AppHandle, hotkey: String) -> Result<(), String> {
    let old = settings_read()["hotkey"].as_str().unwrap_or(DEFAULT_HOTKEY).to_string();
    app.global_shortcut().unregister_all().map_err(|e| e.to_string())?;
    if let Err(e) = register_toggle_hotkey(&app, &hotkey) {
        let _ = register_toggle_hotkey(&app, &old);
        return Err(e);
    }
    let mut s = settings_read();
    s["hotkey"] = serde_json::Value::String(hotkey);
    settings_write(s)
}
```

`run()` 中的热键初始化改为：删除顶部 `let toggle = Shortcut::new(...)` 与 `.with_handler(...)` 写法，插件注册改成 `.plugin(tauri_plugin_global_shortcut::Builder::new().build())`；`setup` 里把 `app.global_shortcut().register(toggle)?;` 替换为：

```rust
            let hotkey = settings_read()["hotkey"]
                .as_str()
                .unwrap_or(DEFAULT_HOTKEY)
                .to_string();
            let handle = app.handle().clone();
            if register_toggle_hotkey(&handle, &hotkey).is_err() {
                // 设置里的热键非法/被占用时退回默认，保证应用总能被唤起。
                let _ = register_toggle_hotkey(&handle, DEFAULT_HOTKEY);
            }
```

`invoke_handler` 列表加入 `settings_read, settings_write, set_hotkey`。
注意 import：`Shortcut` / `ShortcutState` / `GlobalShortcutExt` 已在文件顶部，`Code`、`Modifiers` 若不再使用则从 use 中移除。

Run: `cd src-tauri && cargo test` → Expected: PASS

- [ ] **Step 3: 前端 settings.ts**

创建 `src/lib/settings.ts`：

```ts
import { invoke } from "@tauri-apps/api/core";

export type Settings = {
  hotkey?: string;
  /** pluginId -> 是否允许剪贴板内容匹配时自动打开（默认 true） */
  autoOpen?: Record<string, boolean>;
};

export const readSettings = () => invoke<Settings>("settings_read");
export const writeSettings = (value: Settings) => invoke<void>("settings_write", { value });
export const setHotkey = (hotkey: string) => invoke<void>("set_hotkey", { hotkey });
export const DEFAULT_HOTKEY = "super+shift+space";
```

- [ ] **Step 4: Settings.svelte（热键录制）**

创建 `src/lib/Settings.svelte`：

```svelte
<script lang="ts">
  import { DEFAULT_HOTKEY, setHotkey } from "./settings";

  let {
    hotkey,
    onSaved,
    onError,
  }: {
    hotkey: string;
    onSaved: (hotkey: string) => void;
    onError: (msg: string) => void;
  } = $props();

  let recorded = $state("");
  let recording = $state(false);

  const MOD_KEYS = new Set(["Meta", "Control", "Alt", "Shift"]);

  function keyFromCode(code: string): string {
    if (code.startsWith("Key")) return code.slice(3).toLowerCase();
    if (code.startsWith("Digit")) return code.slice(5);
    return code.toLowerCase(); // Space / Comma / F1 …
  }

  function capture(e: KeyboardEvent) {
    e.preventDefault();
    e.stopPropagation();
    if (MOD_KEYS.has(e.key)) return; // 只按修饰键时等待主键
    const mods = [
      e.metaKey ? "super" : "",
      e.ctrlKey ? "ctrl" : "",
      e.altKey ? "alt" : "",
      e.shiftKey ? "shift" : "",
    ].filter(Boolean);
    if (mods.length === 0) return; // 全局热键必须带修饰键
    recorded = [...mods, keyFromCode(e.code)].join("+");
    recording = false;
  }

  async function save() {
    if (!recorded) return;
    try {
      await setHotkey(recorded);
      onSaved(recorded);
      recorded = "";
    } catch (err) {
      onError(String(err));
    }
  }
</script>

<div class="settings">
  <div class="row">
    <span class="name">唤起热键</span>
    <input
      readonly
      value={recording ? "按下新快捷键…" : recorded || hotkey}
      onfocus={() => (recording = true)}
      onblur={() => (recording = false)}
      onkeydown={capture}
    />
    <button disabled={!recorded} onclick={save}>保存</button>
  </div>
  <div class="hint">点击输入框后按下组合键（需包含 ⌘/⌃/⌥ 修饰键）。默认 {DEFAULT_HOTKEY}。</div>
</div>

<style>
  .settings {
    padding: 12px 14px;
    color: #e8e8ea;
  }
  .row {
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .name {
    font-size: 13px;
    white-space: nowrap;
  }
  input {
    flex: 1;
    border: 0;
    outline: 0;
    background: rgba(255, 255, 255, 0.06);
    color: #fff;
    border-radius: 6px;
    padding: 6px 10px;
    font-size: 13px;
  }
  input:focus {
    outline: 1px solid var(--sel);
  }
  button {
    border: 0;
    background: var(--sel);
    color: #fff;
    border-radius: 6px;
    padding: 6px 12px;
    cursor: pointer;
    font-size: 13px;
  }
  button:disabled {
    opacity: 0.45;
    cursor: default;
  }
  .hint {
    margin-top: 8px;
    color: var(--muted);
    font-size: 12px;
  }
</style>
```

- [ ] **Step 5: App.svelte 接入 settings 模式**

- `mode` 联合类型加 `"settings"`。
- import：`Settings.svelte`、`readSettings` 与 `Settings` 类型（from `./lib/settings`）。
- state：`let appSettings: import("./lib/settings").Settings = $state({});`，`onMount` 中 `appSettings = await readSettings().catch(() => ({}));`
- 打开/关闭：

```ts
  function openSettings() {
    void setAutoHide(false);
    query = "";
    results = [];
    mode = "settings";
  }

  function exitSettings() {
    void setAutoHide(true);
    mode = "search";
    query = "";
    computeResults("");
  }
```

- `goBack` 增加 `else if (mode === "settings") exitSettings();`
- `handleInput` 的 manager/consent 早退分支加上 settings：`if (mode === "manager" || mode === "consent" || mode === "settings") return;`
- 命令表（Task 3 建立）追加一项：

```ts
    { aliases: ["/settings", "/设置"], title: "设置", subtitle: "/settings · /设置", run: () => openSettings() },
```

- bar 标题：`{mode === "manager" ? "插件管理" : mode === "consent" ? "安装插件" : mode === "settings" ? "设置" : activeLabel}`
- 模板中 manager 分支之后加：

```svelte
  {:else if mode === "settings"}
    <Settings
      hotkey={appSettings.hotkey ?? "super+shift+space"}
      onSaved={(hk) => {
        appSettings = { ...appSettings, hotkey: hk };
        showToast("热键已更新：" + hk);
      }}
      onError={(msg) => showToast("热键设置失败：" + msg, "error")}
    />
```

- [ ] **Step 6: 验证 + Commit**

Run: `pnpm test && pnpm check && pnpm build && cd src-tauri && cargo test`
Expected: 全部 PASS

```bash
git add -A && git commit -m "feat: settings view with configurable global hotkey"
```

---

### Task 5: 剪贴板自动打开策略

**Files:**
- Modify: `src/App.svelte`（同一内容只自动打开一次 + 尊重开关）
- Modify: `src/lib/PluginManager.svelte`（每插件"自动打开"开关）

- [ ] **Step 1: App.svelte —— 自动打开策略**

state 增加 `let lastAutoClip = "";`

`refreshClipboard` 改为：

```ts
  async function refreshClipboard() {
    let clip: { kind: string; text: string };
    try {
      clip = await readClipboard();
    } catch {
      clip = { kind: "empty", text: "" };
    }
    const kind = clip.kind === "text" ? detectContentKind(clip.text) : null;
    const matches = kind ? featuresForContent(kind) : [];

    // 默认自动打开，但：同一段剪贴板内容只自动打开一次（Esc 退出后不再劫持）；
    // 用户可在插件管理里按插件关闭自动打开；多个适配插件时只做候选推荐。
    const auto = matches.filter(
      (m) => m.kind === "feature" && appSettings.autoOpen?.[m.plugin.id] !== false,
    );
    if (auto.length === 1 && matches.length === 1 && clip.text !== lastAutoClip) {
      lastAutoClip = clip.text;
      clipRecommendations = [];
      const m = auto[0];
      if (m.kind === "feature") void enterFeature(m.plugin, m.feature);
      return;
    }
    clipRecommendations = matches;
    if (mode === "search" && query.trim() === "") results = clipRecommendations;
  }
```

- [ ] **Step 2: PluginManager 增加自动打开开关**

App.svelte 传参：给 `<PluginManager ... />` 增加：

```svelte
      {plugins}
      autoOpen={appSettings.autoOpen ?? {}}
      onToggleAutoOpen={async (id, value) => {
        appSettings = { ...appSettings, autoOpen: { ...appSettings.autoOpen, [id]: value } };
        await writeSettings(appSettings);
      }}
```

（import `writeSettings` from `./lib/settings`。）

`PluginManager.svelte` props 增加：

```ts
    plugins: Plugin[];
    autoOpen: Record<string, boolean>;
    onToggleAutoOpen: (id: string, value: boolean) => void;
```

（`Plugin` 类型从 `./types` import。）

已安装列表的 `<li>` 内（perms 行之后）为有 content 触发器的插件渲染开关：

```svelte
        {#if plugins.find((x) => x.id === p.id)?.features.some((f) => f.triggers.some((t) => t.kind === "content"))}
          <label class="auto-open">
            <input
              type="checkbox"
              checked={autoOpen[p.id] !== false}
              onchange={(e) => onToggleAutoOpen(p.id, e.currentTarget.checked)}
            />
            剪贴板内容匹配时自动打开
          </label>
        {/if}
```

样式补：

```css
  .auto-open {
    display: flex;
    align-items: center;
    gap: 6px;
    color: var(--muted);
    font-size: 12px;
    margin-top: 4px;
    cursor: pointer;
  }
```

注意：开发模式下 bundled 插件不在 `installed` 里也应可配置——把开关同样渲染进注册中心下方新增一个「本地插件」区不必做；bundled 插件仅开发用，YAGNI，跳过。

- [ ] **Step 3: 验证 + Commit**

Run: `pnpm test && pnpm check && pnpm build` → Expected: PASS

```bash
git add -A && git commit -m "feat: clipboard auto-open runs once per content and is per-plugin switchable"
```

---

### Task 6: 插件状态保持（隐藏不丢工作）

**Files:**
- Modify: `src/App.svelte`（pc:shown 处理）

- [ ] **Step 1: 修改 pc:shown 监听**

`onMount` 中的监听改为：

```ts
    await getCurrentWindow().listen("pc:shown", async () => {
      await tick();
      inputEl?.focus();
      // 插件使用中：保留全部状态（iframe 不销毁），也不做剪贴板推荐劫持。
      if (mode === "plugin") return;
      void listApps().then((a) => (apps = a)); // refresh app list in background
      await refreshClipboard();
    });
```

（删除原来的 `if (mode === "plugin") exitPlugin();`。窗口隐藏本身不卸载 iframe，插件内的编辑状态天然保留；只有 Esc/返回才 `exitPlugin()` 销毁。）

- [ ] **Step 2: 验证 + Commit**

Run: `pnpm check && pnpm build` → Expected: PASS

```bash
git add -A && git commit -m "fix: keep plugin state across hide/show"
```

---

### Task 7: logic 插件结果可交互

**Files:**
- Modify: `src/App.svelte`（选中态、键盘、点击、Enter 复制）
- Modify: `src/lib/host.ts`（导出 writeClipboard）
- Modify: `plugin-docs/host-api.md`（说明 Enter/点击 = 复制 title）

- [ ] **Step 1: host.ts 导出复制能力**

在 core APIs 区加：

```ts
export const writeClipboard = (text: string) => invoke<void>("clipboard_write", { text });
```

- [ ] **Step 2: App.svelte 键盘与渲染**

- import `writeClipboard`。
- state：`let pluginSel = $state(0);`
- `enterFeature` 与 `exitPlugin` 中重置：`pluginResults = []; pluginSel = 0;`（enterFeature 里已有 pluginResults 清零，补 pluginSel）。mountPlugin 的 `onSetResults` 回调改为 `(r) => { pluginResults = r; pluginSel = 0; }`。
- 复制动作：

```ts
  function copyPluginResult(r: any) {
    const text = String(r?.title ?? r ?? "");
    if (!text) return;
    void writeClipboard(text).then(
      () => showToast("已复制"),
      (e) => showToast("复制失败：" + String(e), "error"),
    );
  }
```

- `onKeydown` 中把 `if (mode === "plugin") return;` 替换为：

```ts
    if (mode === "plugin") {
      if (activeFeatureType === "logic" && pluginResults.length > 0) {
        if (e.key === "ArrowDown") {
          e.preventDefault();
          pluginSel = Math.min(pluginSel + 1, pluginResults.length - 1);
        } else if (e.key === "ArrowUp") {
          e.preventDefault();
          pluginSel = Math.max(pluginSel - 1, 0);
        } else if (e.key === "Enter") {
          e.preventDefault();
          copyPluginResult(pluginResults[pluginSel]);
        }
      }
      return;
    }
```

- 模板 logic 结果列表改为：

```svelte
      <ul class="results">
        {#each pluginResults as r, i (i)}
          <li
            class:sel={i === pluginSel}
            onmousedown={() => copyPluginResult(r)}
            role="option"
            aria-selected={i === pluginSel}
            tabindex="-1"
          >
            <span class="meta">
              <span class="title">{r.title ?? r}</span>
              <span class="sub">{r.subtitle ?? ""}</span>
            </span>
          </li>
        {/each}
      </ul>
```

- [ ] **Step 3: 文档补一句**

`plugin-docs/host-api.md` 的 `host.setResults(results)` 小节末尾补充：

```markdown
用户可用 `↑/↓` 在结果间移动，`Enter` 或点击会把该项的 `title` 复制到剪贴板。
```

- [ ] **Step 4: 验证 + Commit**

Run: `pnpm check && pnpm build` → Expected: PASS

```bash
git add -A && git commit -m "feat: keyboard and click interaction for logic plugin results"
```

---

### Task 8: 插件管理器打磨（名称、卸载确认、注册中心状态与并行加载）

**Files:**
- Modify: `src/App.svelte`（卸载确认、openManager 不阻塞、并行刷新）
- Modify: `src/lib/PluginManager.svelte`（显示名称、安装状态、加载中）

- [ ] **Step 1: 卸载确认（App.svelte）**

import：`import { ask } from "@tauri-apps/plugin-dialog";`

```ts
  async function doUninstall(id: string) {
    const name = plugins.find((p) => p.id === id)?.name ?? id;
    const yes = await ask(`确定卸载「${name}」？插件的数据也会一并删除。`, { title: "卸载插件" });
    if (!yes) return;
    await uninstallPlugin(id);
    plugins = await listPlugins();
    await refreshInstalled();
    showToast("已卸载");
  }
```

- [ ] **Step 2: openManager 立即切视图 + 并行刷新（App.svelte）**

```ts
  let registryLoading = $state(false);

  async function refreshRegistries() {
    registryLoading = true;
    try {
      registries = registriesWithOfficial(await listRegistries());
      const settled = await Promise.allSettled(registries.map((url) => fetchRegistry(url)));
      const items: RegistryPlugin[] = [];
      const failed: string[] = [];
      settled.forEach((r, i) => {
        if (r.status === "fulfilled") items.push(...r.value.plugins);
        else failed.push(registries[i]);
      });
      registryPlugins = items;
      if (failed.length > 0) showToast(`注册中心刷新失败：${failed.join("、")}`, "error");
    } finally {
      registryLoading = false;
    }
  }

  async function openManager() {
    void setAutoHide(false); // keep window alive for file dialog / drag-drop
    query = "";
    results = [];
    mode = "manager";
    void refreshInstalled();
    void refreshRegistries();
  }
```

给 `<PluginManager>` 传 `loading={registryLoading}`（以及 Task 5 已传的 `{plugins}`）。

- [ ] **Step 3: PluginManager —— 名称、状态、加载中**

props 增加 `loading: boolean;`（Task 5 已加 `plugins`）。

名称解析 + 安装状态（script 中）：

```ts
  const nameOf = (id: string) => plugins.find((x) => x.id === id)?.name ?? id;

  function regStatus(p: RegistryPlugin): "none" | "installed" | "update" {
    const cur = installed.find((i) => i.id === p.id);
    if (!cur) return "none";
    return cur.version === p.version ? "installed" : "update";
  }
```

- 「刷新注册中心」按钮：`disabled={loading}`，文案 `{loading ? "刷新中…" : "刷新注册中心"}`。
- 注册中心插件行的安装按钮改为：

```svelte
              {#if regStatus(p) === "installed"}
                <button disabled>已安装</button>
              {:else if regStatus(p) === "update"}
                <button onclick={() => onInstallRegistryPlugin(p)}>更新到 v{p.version}</button>
              {:else}
                <button onclick={() => onInstallRegistryPlugin(p)}>安装</button>
              {/if}
```

按钮 disabled 样式补：

```css
  .bar button:disabled,
  .list button:disabled {
    opacity: 0.45;
    cursor: default;
  }
```

（若 `.list` 内按钮原本无选择器约束，直接用 `button:disabled` 全局亦可。）

- 已安装列表显示名称：`<span class="name">{p.id}</span>` 改为 `<span class="name">{nameOf(p.id)}</span>`，并把 id 挪进次要信息：`<span class="ver">v{p.version} · {p.source} · {p.id}</span>`。

- [ ] **Step 4: 验证 + Commit**

Run: `pnpm test && pnpm check && pnpm build` → Expected: PASS

```bash
git add -A && git commit -m "feat: plugin manager polish - names, uninstall confirm, registry status and parallel refresh"
```

---

### Task 9: README 与文档更新

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 更新 README 功能描述**

- 「唤起 / 隐藏」一节：热键描述改为「默认 `Cmd+Shift+Space`，可在 `/设置` 中修改」。
- 「启动应用」一节：补充「支持拼音全拼 / 首字母（`wx` → 微信）与模糊匹配（`vsc` → Visual Studio Code），按使用频率排序」。
- 「内容感知」一节：补充「同一段剪贴板内容只自动打开一次；可在插件管理中按插件关闭自动打开」。
- 新增一行：「**命令**：输入 `/` 调用软件能力——`/插件`（插件管理）、`/设置`（热键等设置）」。

- [ ] **Step 2: 验证 + Commit**

Run: `pnpm test`（确保 brand 等文档相关自检仍过）

```bash
git add README.md && git commit -m "docs: document pinyin search, slash commands, settings"
```

---

## Self-Review 记录

- 规格覆盖：12 项确认意见 → 任务映射：1→T1，2→T2，3→T5，4→T6，5/6/8→T3，7→T3+T4（命令表分两步建），9→T7，10/11→T8，12→T4，文档→T9。无遗漏。
- 类型一致性：`ResultItem` 的 `command` 变体在 T3 定义、T3/T4 使用；`appSettings` 在 T4 定义、T5 使用；`plugins` prop 在 T5 传入、T8 复用；`showToast(msg, kind)` 在 T3 定义、T4/T5/T7/T8 使用。任务必须按序执行。
- 已知取舍：frecency 存 localStorage（够用，将来要跨设备再迁 settings.json）；拼音只覆盖 macOS（应用本身仅 macOS）；logic 结果 Enter 固定为复制 title（契约里只有 title/subtitle）。
